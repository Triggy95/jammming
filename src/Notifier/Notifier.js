import React, { useEffect, useState } from 'react';

import styles from 'src\Notifier\Notifier.module.css';

export default function Notifier({ hidden }) {
    const [isAbsolute, setAbsolute] =useState(true);
    useEffect(() => {
        
    })
}